# Obsera Development Environment

## How to Use

1. Clone this repo
2. Put/clone obsera source code to into this folder. Make sure its name is _obsera_.
3. Put all *.sql of required database into mysql/init folder
4. Remove folder db in mysql folder in exists.
5. Run `docker compose up --build`
6. Access obsera from `http://localhost:8000`
